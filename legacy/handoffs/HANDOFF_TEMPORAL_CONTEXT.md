# Handoff: Temporal Context + Settings Idea

## Статус: Issue #3 закрыт ✅

SoloBuddy теперь понимает стадию BIP-пути пользователя.

---

## Последняя сессия: Temporal Context (2026-01-13)

### Что сделано

**Issue #3: Временной контекст** — ЗАКРЫТ

```
hub/data/user-context.json
  + bipStartDate: "2026-01-10"

hub/prompt-builder.js
  + loadPostsCount() — авто-подсчёт из my-posts.json
  + loadUserContext() — обогащает контекст postsCount
  + buildUserContextSection() — выводит BIP-путь в промпт
```

**Логика стадий:**
| Стадия | Условие |
|--------|---------|
| новичок | <30 дней И <15 постов |
| растущий | 30-90 дней ИЛИ 15-50 постов |
| опытный | >90 дней ИЛИ >50 постов |

**Результат в промпте:**
```
- BIP-путь: 3 дней (новичок), 1 постов
```

**Debug endpoint:**
```
GET /api/debug/user-context
→ { bipStartDate, postsCount, bipLine, fullSection }
```

### Коммиты
```
732ebc5 feat(context): add temporal BIP journey context
5a67a08 chore: add debug endpoint for user-context validation
```

---

## Новая идея: Settings UI

### Проблема

Данные онбординга (user-context.json) и проектов (project-souls/*.json) нельзя редактировать после создания. Пользователь должен лезть в JSON вручную.

Примеры:
- Фолловеры изменились: `Twitter (0)` → `Twitter (150)`
- Добавился новый проект
- Проект перешёл из shotgun в sniper фазу
- Изменился bipStartDate (ошибся при вводе)

### Решение: Settings Panel

**Уровень 1: Hub Settings**
- Редактирование user-context.json через UI
- Поля: phase, platforms (followers), skills, timePerDay, goals, bipStartDate
- Кнопка "Re-run onboarding" для полного сброса

**Уровень 2: Per-Project Settings**
- Редактирование soul для каждого проекта
- Переключение фазы проекта (shotgun/sniper/exploring)
- Привязка к платформам

### Архитектура

```
┌─────────────────────────────────────────────┐
│ Settings (шестерёнка в header)              │
├─────────────────────────────────────────────┤
│ Tab 1: Profile                              │
│   - BIP Start Date                          │
│   - Phase (shotgun/sniper)                  │
│   - Time per day                            │
│   - Goals priority                          │
├─────────────────────────────────────────────┤
│ Tab 2: Platforms                            │
│   - Twitter: [followers input]              │
│   - Telegram: [followers input]             │
│   - ...                                     │
├─────────────────────────────────────────────┤
│ Tab 3: Projects                             │
│   - [Project 1] - phase, soul edit          │
│   - [Project 2] - phase, soul edit          │
└─────────────────────────────────────────────┘
```

### API endpoints

```
GET  /api/settings              → текущие настройки
PUT  /api/settings              → обновить user-context
GET  /api/settings/project/:id  → настройки проекта
PUT  /api/settings/project/:id  → обновить soul проекта
```

---

## Что дальше: Приоритеты

### 🔴 Критично

1. ~~**Временной контекст** (Issue #3)~~ ✅ DONE

2. **Per-project phases**
   - Сейчас одна фаза на всё
   - Нужно: SPHERE=sniper, VOP=shotgun

### 🟡 Важно

3. **Settings UI** (новая идея)
   - Редактирование user-context
   - Редактирование project souls
   - Без ручного JSON

4. **my-posts.json в контекст**
   - Buddy видит что опубликовано
   - Может предлагать follow-up

### 🟢 Nice to have

5. Language Toggle — ручной EN/RU
6. Style Learning — обучение по опубликованным постам

---

## Текущее состояние

| Компонент | Статус |
|-----------|--------|
| Temporal Context | ✅ Работает |
| Chat API | ✅ V2 |
| Content Generation | ✅ V1 |
| User Onboarding | ✅ Read-only |
| Project Voice | ✅ Read-only |
| Settings UI | ❌ Нет |

---

## Как запустить

```bash
cd hub && npm start
# http://localhost:3000
```

**Проверка temporal context:**
```
http://localhost:3000/api/debug/user-context
```

---

**Репозиторий:** https://github.com/Humanji7/solobuddy
