# Handoff — 2026-01-12

## Сессия

### Knowledge Item: Hybrid Intent Detection
Создана KI для паттерна гибридного распознавания интентов:

```
/knowledge/hybrid_intent_detection/
├── metadata.json
└── artifacts/
    ├── overview.md              # 3-tier architecture
    ├── implementation_patterns.md  # Code examples
    └── confidence_scoring.md    # Scoring algorithm
```

**Паттерн:** Regex ≥80% → LLM 50-79% → Chat <50%

### System Prompt V2
Создан `system-prompt-v2.md` — полный редизайн промпта для BIP Buddy:

- **Role:** Стратегический партнёр + тактический советник (не коуч)
- **Context:** Shotgun-фаза, 30-45 мин/день на контент
- **Philosophy:** Shotgun-to-Sniper как система поиска PMF
- **Tactics:** Ежедневно/еженедельно/ежемесячно breakdown
- **Signals:** Сильные vs слабые сигналы для решения о фокусе
- **Modes:** Тактика / Стратегия / Фидбек / Анализ

## Файлы

| Файл | Статус |
|------|--------|
| `system-prompt-v2.md` | NEW — промпт v2 |
| `/knowledge/hybrid_intent_detection/` | NEW — KI |

## Known Issues (из прошлого handoff)

- [ ] Empty catch blocks в `routes/chat.js` (lines 40, 108, 119)
- [ ] `type` vs `intentType` inconsistency

## Следующие шаги

1. **Интегрировать system-prompt-v2** в `prompt-builder.js`
2. **Fix minor issues** — catch blocks + naming
3. **Test new prompt** — E2E с разными сценариями

---

## Сессия 2: User Context Onboarding ✅

### Имплементация завершена

Реализован User Context Onboarding Wizard по паттерну `soul-onboarding.js`.

#### Решения:
- **Test infrastructure:** Нативные тесты (no Jest)
- **Endpoint location:** Отдельный `routes/user.js` (separation of concerns)

#### Созданные файлы:

| Файл | Статус | Описание |
|------|--------|----------|
| `hub/data/user-context.json` | ✅ | JSON template |
| `hub/routes/user.js` | ✅ | GET/POST `/api/user-context` |
| `hub/user-onboarding.js` | ✅ NEW | 530 lines, 8-step wizard |
| `hub/user-onboarding.css` | ✅ NEW | 500 lines, glassmorphism |
| `hub/index.html` | ✅ MOD | CSS/JS includes |
| `hub/server.js` | ✅ MOD | userRoutes mount |
| `hub/app.js` | ✅ MOD | auto-trigger on DOMContentLoaded |

#### Wizard Steps:
1. **Intro** — Welcome + feature preview
2. **Phase** — Shotgun / Sniper / Exploring
3. **Projects** — Add projects with status (idea/building/live)
4. **Platforms** — Twitter, Telegram, etc + follower count
5. **Skills** — Developer, AI Creator, Video, Writer, etc
6. **Time** — 15/30/45/60+ min per day
7. **Goals** — Discipline, Networking, Sales, PMF, etc
8. **Preview** — Summary + language selection + save

### E2E Verification ✅

- Wizard auto-triggers при `onboarded: false`
- Все 8 шагов работают корректно
- UI рендерится с glassmorphism styling
- Навигация Back/Next работает
- Server перезапущен с новыми routes

### Следующие шаги

1. **Prompt Integration:**
   - [x] Модифицировать `buildSystemPrompt()` для загрузки `user-context.json`
   - [x] Построение `<user_context>` секции динамически из wizard data

2. **Known Issues (из прошлого handoff):**
   - [ ] Empty catch blocks в `routes/chat.js`
   - [ ] `type` vs `intentType` inconsistency

---

## Сессия 3: System Prompt V2 Integration ✅

### Имплементация завершена

Интегрирован `system-prompt-v2.md` в `prompt-builder.js` с динамической подстановкой user context из onboarding wizard.

#### Добавленные функции:

| Функция | Назначение |
|---------|-----------|
| `loadUserContext()` | Загружает `user-context.json` и проверяет `onboarded: true` |
| `buildUserContextSection()` | Строит `<user_context>` секцию из wizard data с маппингом полей |
| `buildSystemPromptV2()` | **ASYNC** — загружает `system-prompt-v2.md`, заменяет `<user_context>`, добавляет language enforcement |

#### Маппинг данных wizard → prompt:

- **Phase:** `shotgun/sniper/exploring` → описание фазы на русском
- **Projects:** массив с `name` + `status` → "проект1 (✅ live), проект2 (🔧 building)"
- **Platforms:** массив с `id` + `followers` → "Twitter (4), Telegram (20)"
- **Skills:** `ai_creator`, `developer` и т.д. → русские названия
- **Time:** `timePerDay` → "45 минут в день на контент"
- **Goals:** `discipline`, `networking`, `sales` → "дисциплина публичности → нетворкинг → продажи"
- **Language:** `ru`/`en` → "русский" / "English"

#### Изменённые файлы:

| Файл | Статус | Изменения |
|------|--------|-----------|
| `hub/prompt-builder.js` | ✅ MOD | +120 lines: 3 новые функции + updated exports |

### Архитектура V2:

```
buildSystemPromptV2(options)
  ↓
1. Загрузить system-prompt-v2.md как template
2. Загрузить user-context.json (если onboarded: true)
3. Динамически построить <user_context> через buildUserContextSection()
4. Заменить статический <user_context> в template на динамический
5. Добавить language enforcement (detect из userMessage)
6. Вернуть полный промпт
```

### Следующие шаги

1. **Integration into chat-api.js:**
   - [ ] Переключить `chat-api.js` на использование `buildSystemPromptV2()` вместо `buildSystemPrompt()`
   - [ ] Передать `userMessage` в опциях для language detection

2. **E2E Testing:**
   - [ ] Тест "новый юзер" → wizard → сохранение → проверка промпта
   - [ ] Тест разных комбинаций wizard data
   - [ ] Тест language detection (ru/en сообщения)

3. **Known Issues (из прошлого handoff):**
   - [ ] Empty catch blocks в `routes/chat.js`
   - [ ] `type` vs `intentType` inconsistency

---

## Сессия 4: Groq Gate ✅

### Проблема (была)
Regex-триггеры не покрывали вариативность естественного языка:
- "let's write a thread about X" → 0% (не срабатывало)
- "what we have to say about X?" → 0%
- LLM fallback только для 50-79%, а 0-49% игнорировался

### Решение: Groq Gate

```
Message → Regex (1ms) → generate_content ≥80%? → ContentCard
                ↓ <80%
         Groq Gate (~100ms) → is_content_request? → ContentCard
                ↓ no
         Claude Chat (fallback)
```

**Scope:** Только `generate_content`. Остальные интенты через chat.

### Созданные файлы

| Файл | Статус | Описание |
|------|--------|----------|
| `hub/groq-classifier.js` | ✅ NEW | Groq API classifier (Llama 3.1 8B) |
| `hub/routes/chat.js` | ✅ MOD | Groq Gate integration |
| `hub/.env.example` | ✅ MOD | +GROQ_API_KEY |

### Тестирование

| Фраза | Было | Стало |
|-------|------|-------|
| `lets write a fun thread about solobuddy` | ❌ 0% | ✅ 92% groq |
| `what we have to say about solobuddy?` | ❌ 0% | ✅ 92% groq |
| `So what im doing today? Make a post` | ❌ 0% | ✅ 92% groq |
| `привет, как дела?` | none | ✅ none (correct) |

### Характеристики
- **Latency:** ~100-300ms
- **Cost:** ~$0 (Groq free tier: 30 req/min)
- **Model:** Llama 3.1 8B Instant

---

## Следующие шаги

1. **Integration into chat-api.js:**
   - [ ] Переключить `chat-api.js` на использование `buildSystemPromptV2()`
   - [ ] Передать `userMessage` в опциях для language detection

2. **E2E Testing system-prompt-v2:**
   - [ ] Тест разных комбинаций wizard data
   - [ ] Тест language detection (ru/en)

3. **Known Issues:**
   - [ ] Empty catch blocks в `routes/chat.js`
   - [ ] `type` vs `intentType` inconsistency

