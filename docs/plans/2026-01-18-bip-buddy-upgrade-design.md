# BIP Buddy Upgrade — Twitter Copilot

> Углубление SoloBuddy как партнёра по Build in Public

## Статус

- **Дата:** 2026-01-18
- **Статус:** Draft → Ready for implementation

## Контекст

**Текущее состояние:**
- Jester-Sage голос (ироничный, философский)
- Twitter Scout: мониторит watchlist → фильтрует → советует в Telegram
- Workflow контента: ideas → drafts → publish

**Что добавляем:**
- Аналитика своего профиля (MIRROR)
- Трекинг роста
- Контент-календарь с напоминаниями
- Умные драфты

**Принципы:**
- Human-in-the-loop — никакого автопостинга
- Только Twitter (YAGNI для других платформ)
- Bird CLI + скрапинг (без платного API)
- MVP всего сразу, потом углублять

## Архитектура

```
┌─────────────────────────────────────────────────────┐
│                    BIP Buddy                         │
├──────────────────────┬──────────────────────────────┤
│   SCOUT (есть)       │   MIRROR (новое)             │
│   Чужие твиты        │   Свой профиль               │
├──────────────────────┼──────────────────────────────┤
│ watchlist tweets     │ my tweets + metrics          │
│ engagement opps      │ followers history            │
│ → "ответь этому"     │ engagement trends            │
│                      │ → "твой твит взлетает"       │
└──────────────────────┴──────────────────────────────┘
                       ↓
              SQLite (data/bip.db)
                       ↓
              ClawdBot Gateway
                       ↓
                   Telegram
```

## База данных (SQLite)

```sql
-- Снимки твитов с метриками
CREATE TABLE tweets (
  id INTEGER PRIMARY KEY,
  tweet_id TEXT UNIQUE,
  content TEXT,
  created_at DATETIME,
  snapshot_at DATETIME,
  likes INTEGER,
  replies INTEGER,
  retweets INTEGER,
  views INTEGER
);

-- История профиля
CREATE TABLE profile_snapshots (
  id INTEGER PRIMARY KEY,
  snapshot_at DATETIME,
  followers INTEGER,
  following INTEGER
);

-- Драфты и контент-календарь
CREATE TABLE drafts (
  id INTEGER PRIMARY KEY,
  content TEXT,
  source TEXT,            -- 'session', 'watchlist', 'manual'
  source_ref TEXT,
  status TEXT,            -- 'idea', 'draft', 'reminded', 'posted', 'dropped'
  remind_at DATETIME,
  created_at DATETIME,
  posted_at DATETIME,
  posted_tweet_id TEXT
);

-- Дедупликация алертов
CREATE TABLE alerts_sent (
  id INTEGER PRIMARY KEY,
  alert_type TEXT,
  ref_id TEXT,
  sent_at DATETIME
);
```

## Сбор данных

**Новый скрипт: `twitter-mirror.sh`**

Cron каждые 2 часа:
1. `bird user-tweets Toporcalibur -n 20 --json` → tweets table
2. Сравнение с предыдущим снимком → дельта
3. Триггер алертов если нужно

**Скрипт: `twitter-digest.sh`**

По запросу (`/digest`) или cron раз в день:
- Агрегация метрик
- Расчёт engagement rate
- Генерация инсайтов

**Частота сбора:**

| Данные | Частота | Хранение |
|--------|---------|----------|
| Мои твиты (метрики) | каждые 2ч | 30 дней детально |
| Профиль (followers) | раз в день | бессрочно |
| Watchlist (чужие) | каждые 30м | 7 дней |

## Алерты (push)

Триггеры для немедленной отправки:
- Твит набрал +50 лайков за 2 часа (выше среднего)
- Followers +10 за день
- Упоминание от аккаунта с >10K фолловеров

Формат:
```
🚀 Твой твит взлетает!
"Потратил 3 часа на баг..."
+87 лайков за 2 часа (твой средний: 23)
→ x.com/Toporcalibur/status/...
```

## Telegram-интерфейс

**Команды:**

| Команда | Что делает |
|---------|------------|
| `/draft <текст>` | Сохранить драфт |
| `/drafts` | Список драфтов в очереди |
| `/stats` | Краткая сводка: followers, engagement за неделю |
| `/top` | Топ-5 твоих твитов за период |
| `/digest` | Полный отчёт: рост, тренды, рекомендации |
| `/posted <draft_id>` | Отметить драфт как опубликованный |

**Workflow драфтов:**

```
Ты: /draft Потратил 3 часа на баг. Баг был в одной букве.
Бот: 📝 Сохранил драфт #7.
     Напомнить позже? [Завтра 9:00] [Без напоминания]

--- завтра 9:00 ---
Бот: 🔔 Напоминание о драфте #7:
     "Потратил 3 часа на баг..."
     [Постить буду сам] [Отложить] [Удалить]
```

**Дайджест:**

```
📊 BIP Buddy — неделя #3

Твиты: 12 опубликовано
Лучший: "..." — 234 лайка
Худший: "..." — 8 лайков

Фолловеры: 342 → 389 (+47)
Engagement rate: 4.2% (+0.8%)

💡 Инсайт: твиты с кодом получают
   в 2x больше engagement чем текстовые.

Драфтов в очереди: 3
```

## План реализации

**Фаза 1 — Фундамент:**
- [ ] SQLite схема + таблицы
- [ ] `twitter-mirror.sh` — сбор своих метрик
- [ ] Cron каждые 2 часа
- [ ] Базовые алерты (твит взлетает, рост фолловеров)

**Фаза 2 — Драфты:**
- [ ] Команда `/draft` в ClawdBot
- [ ] Таблица drafts + напоминания
- [ ] `/drafts`, `/posted`

**Фаза 3 — Аналитика:**
- [ ] `/stats`, `/top`, `/digest`
- [ ] Расчёт engagement rate, тренды
- [ ] Инсайты ("твиты с кодом работают лучше")

**Фаза 4 — Умные предложения:**
- [ ] Парсинг `session-log.md` → предложения драфтов
- [ ] Связка watchlist → "вот угол для ответа"
- [ ] Оптимальное время на основе истории

## YAGNI (не делаем)

- Автопостинг
- Другие платформы (LinkedIn, Threads, BlueSky)
- Twitter API (платный)
- Сложная ML-аналитика
- Демография аудитории

## Файловая структура

```
.ai/scripts/
├── twitter-monitor.sh    # Scout (есть)
├── twitter-analyze.sh    # Scout анализ (есть)
├── twitter-mirror.sh     # Mirror (новое)
└── twitter-digest.sh     # Дайджест (новое)

data/
├── twitter/              # существующее
└── bip.db                # SQLite (gitignored)
```
