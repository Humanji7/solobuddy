# SoloBuddy → ClawdBot Migration Design

> Стратегический разворот: переход от собственной инфраструктуры к ClawdBot skills

**Дата:** 2026-01-14
**Статус:** Draft
**Автор:** gHashTag + Claude

---

## 1. Контекст и мотивация

### Текущее состояние SoloBuddy

```
bip-buddy/hub/
├── server.js           # Express сервер
├── chat-api.js         # Claude API integration
├── prompt-builder.js   # System prompts (~870 LOC)
├── soul-*.js           # Soul system
├── github-api.js       # OAuth + posting
└── index.html          # Web UI
```

**Проблема:** Разработка и поддержка собственной инфраструктуры отвлекает от главного — создания контента.

### Почему ClawdBot

ClawdBot даёт бесплатно:

| Функция | SoloBuddy (своё) | ClawdBot (готово) |
|---------|------------------|-------------------|
| Multi-channel | Нет | WhatsApp, Telegram, Discord, Slack... |
| Browser control | Нет | Playwright встроен |
| Cron/webhooks | Нет | Есть |
| Twitter | В backlog | `bird` skill готов |
| Распространение | Нет | ClawdHub registry |

**Решение:** Перенести логику SoloBuddy в ClawdBot skills.

---

## 2. Архитектура

### Гибридная структура skills

```
~/clawd/skills/
├── solobuddy/              # Core skill
│   ├── SKILL.md
│   └── prompts/
│       ├── system.md
│       ├── content.md
│       ├── project-voice.md
│       ├── soul-creator.md
│       └── profile.md
│
└── solobuddy-twitter/      # Extension
    └── SKILL.md
```

**Почему гибрид:**
- Core покрывает 90% задач (backlog, генерация, GitHub)
- Twitter отдельно — другие зависимости (`bird` CLI)
- Можно использовать core без Twitter

### Данные

**Остаются в:** `/Users/admin/projects/bip-buddy`

```
bip-buddy/
├── ideas/
│   ├── backlog.md          # Идеи контента
│   └── session-log.md      # Захват мыслей
├── drafts/                 # Черновики
├── data/
│   ├── project-souls/      # Soul JSON файлы
│   └── my-posts.json       # Опубликованные посты
├── PROFILE.md              # Голос Jester-Sage
└── system-prompt-v2.md     # BIP Strategic Partner
```

**Конфиг ClawdBot:**

```json
// ~/.clawdbot/clawdbot.json
{
  "solobuddy": {
    "dataPath": "/Users/admin/projects/bip-buddy",
    "githubRepo": "gHashTag/solobuddy-content"
  },
  "twitter": {
    "watchlist": ["@levelsio", "@marc_louvion", "@naval"],
    "checkInterval": "30m"
  }
}
```

---

## 3. Core Skill: solobuddy

### SKILL.md Header

```yaml
---
name: solobuddy
description: Build-in-public content workflow — backlog, drafts, generation, publishing
homepage: https://github.com/gHashTag/bip-buddy
metadata: {"clawdbot":{"emoji":"🎯","requires":{"bins":["gh"]},"config":["solobuddy.dataPath"]}}
---
```

### Команды

#### Backlog (идеи)
- `backlog` / `идеи` — показать список
- `добавь идею <текст>` — добавить в backlog.md
- `удали идею <номер>` — убрать

#### Session Log (захват)
- `лог` / `session` — последние записи
- `запиши <текст>` — добавить с датой

#### Drafts (черновики)
- `черновики` — список файлов
- `открой черновик <имя>` — показать
- `сохрани черновик <имя>` — сохранить

#### Генерация
- `сгенери пост из идеи <номер>` — Claude пишет пост
- `сгенери тред из идеи <номер>` — Claude пишет thread
- `переделай` — регенерация

#### Публикация
- `опубликуй в github` — коммит в репо
- `статус github` — проверить auth

#### Soul System
- `создай душу для <путь>` — анализ .md файлов проекта, создание SOUL
- `покажи душу <проект>` — текущий SOUL
- `спроси у <проект>` — two-agent консультация

### Soul Creation Flow (через Telegram кнопки)

```
User: создай душу для ~/projects/sphere

Bot: 🔍 Сканирую проект sphere...

     Найдено:
     - README.md ✓
     - CLAUDE.md ✓
     - docs/PHILOSOPHY.md ✓
     - docs/ARCHITECTURE.md ✓
     - 12 других .md файлов

     Шаг 1/5: Кто я по натуре?

     [🌀 Существо] [🔧 Инструмент] [🧭 Проводник] [🎨 Художник]

User: [нажимает "Существо"]

Bot: ✓ Существо

     Шаг 2/5: Как я говорю? (можно несколько)

     [Игриво] [Технично] [Поэтично] [Спокойно] [Интенсивно]

... (5 шагов) ...

Bot: 📜 SOUL создана на основе 17 документов:

     Name: SPHERE
     Essence: Живое существо, не портфолио
     Voice: Визуальный, тактильный, исследовательский
     Philosophy: "Сайт как организм, который дышит"
     ...

     [💾 Сохранить] [✏️ Редактировать]
```

### Two-Agent Consultation

```
User: спроси у sphere что написать сегодня

Bot: 🤖 Запускаю консультацию...

     [SoloBuddy Agent]: У нас в backlog 3 идеи про SPHERE.
     Какая лучше зайдёт аудитории?

     [SPHERE Soul]: Идея #7 про eye-tracking —
     самая визуальная. Можно показать GIF.
     Это цепляет больше чем текст про архитектуру.

     [SoloBuddy]: Согласен. Формат — короткий пост + GIF.

     📋 Рекомендация: Пост про eye-tracking с демо-GIF

     [✍️ Сгенерить черновик] [➡️ Другая идея]
```

### Промпты (перенос из существующего)

| Источник | Назначение |
|----------|------------|
| `prompt-builder.js:buildSystemPrompt()` | `prompts/system.md` |
| `prompt-builder.js:buildContentPrompt()` | `prompts/content.md` |
| `prompt-builder.js:buildProjectVoicePrompt()` | `prompts/project-voice.md` |
| `PROFILE.md` | `prompts/profile.md` |
| NEW | `prompts/soul-creator.md` |

---

## 4. Extension: solobuddy-twitter

### SKILL.md Header

```yaml
---
name: solobuddy-twitter
description: Twitter engagement assistant — proactive opportunities with draft comments
metadata: {"clawdbot":{"emoji":"🐦","requires":{"bins":["bird"]},"config":["twitter.watchlist"]}}
---
```

### Концепция

**Снимает три когнитивных нагрузки:**

1. **Поиск** — где комментить? → Claude мониторит watchlist
2. **Решение** — стоит ли? → Claude объясняет почему
3. **Написание** — что сказать? → Claude предлагает драфт

**Пользователь пассивен — Claude активен.**

### Workflow

```
[Cron каждые 30 мин]
        ↓
bird search "@levelsio" --since 30m
bird search "@naval" --since 30m
...
        ↓
Claude анализирует:
- Резонанс с PROFILE.md
- Engagement потенциал
- Свежесть твита
        ↓
Notification в Telegram
```

### Формат предложения

```
┌─────────────────────────────────────────┐
│ 🎯 Возможность для engagement           │
│                                         │
│ @levelsio написал (15 мин назад):       │
│ "Building in public is lonely.          │
│ Nobody tells you that part."            │
│ 🔗 twitter.com/levelsio/status/...      │
│                                         │
│ 💡 Почему это хорошая возможность:      │
│ • Резонирует с твоим опытом — ты сам    │
│   писал про одиночество в BIP           │
│ • Свежий твит (15 мин) — ранние         │
│   комментарии получают больше внимания  │
│ • Levelsio активно отвечает на          │
│   thoughtful комментарии                │
│ • Тема пересекается с твоим SPHERE —    │
│   можно связать с "живым существом      │
│   которому тоже одиноко"                │
│                                         │
│ ✍️ Предлагаю написать:                  │
│ "The loneliness hits different at 3am   │
│ debugging a bug nobody will ever see.   │
│ But then someone stars your repo and    │
│ suddenly the silence has a witness."    │
│                                         │
│ 🎯 Почему этот текст:                   │
│ • Твой голос (Jester-Sage) — уязвимость │
│   + наблюдение                          │
│ • Добавляет к разговору, не повторяет   │
│ • Заканчивается образом — запоминается  │
│                                         │
│ [✏️ Переписать] [➡️ Следующий]          │
└─────────────────────────────────────────┘
```

### Действия пользователя

1. Видит предложение в Telegram
2. Копирует текст (или просит переписать)
3. Сам открывает ссылку на твит
4. Сам вставляет комментарий в Twitter

**Никакой автоматической отправки** — полный контроль.

---

## 5. Интерфейс

### Основной канал: Telegram

- Текст + inline кнопки
- Полностью заменяет web UI
- Достаточный уровень интерактивности

### Альтернативы (работают автоматически)

- WhatsApp
- Discord
- WebChat ClawdBot

---

## 6. Миграция данных

### Не требуется

Данные остаются на месте:

```
~/projects/bip-buddy/
├── ideas/backlog.md        ← skill читает/пишет
├── ideas/session-log.md    ← skill читает/пишет
├── drafts/                 ← skill читает/пишет
├── data/project-souls/     ← skill читает/пишет
└── PROFILE.md              ← skill читает
```

Skills обращаются по пути из конфига `solobuddy.dataPath`.

### Hub deprecation

После миграции:
- `hub/` можно архивировать
- `server.js` больше не нужен
- Web UI (`index.html`) не используется

---

## 7. План имплементации

### Phase 1: Core Skill

1. Создать `~/clawd/skills/solobuddy/SKILL.md`
2. Перенести промпты из `prompt-builder.js`
3. Реализовать команды backlog/drafts/генерация
4. Протестировать в Telegram

### Phase 2: Soul System

1. Адаптировать soul-onboarding под чат
2. Реализовать `создай душу` с Telegram кнопками
3. Реализовать two-agent consultation через `sessions_send`

### Phase 3: Twitter Extension

1. Создать `~/clawd/skills/solobuddy-twitter/SKILL.md`
2. Настроить cron для мониторинга
3. Реализовать формат предложений с объяснением логики

### Phase 4: Cleanup

1. Архивировать `hub/`
2. Обновить документацию
3. Удалить зависимости Express/etc

---

## 8. Риски и митигация

| Риск | Митигация |
|------|-----------|
| Lock-in в ClawdBot | Skills = markdown, промпты переносимы |
| ClawdBot API changes | Skills версионируются в ClawdHub |
| bird CLI недоступен | Fallback на browser control |

---

## 9. Acceptance Criteria

- [ ] Команда `backlog` показывает идеи из `ideas/backlog.md`
- [ ] Команда `сгенери пост` создаёт контент в голосе Jester-Sage
- [ ] Команда `создай душу` проходит 5-шаговый wizard в Telegram
- [ ] Twitter extension присылает предложения с объяснением логики
- [ ] Вся функциональность работает без `hub/server.js`

---

## Appendix A: Существующие souls

```
data/project-souls/
├── sphere-777.json     # Живое существо, playful + calm
├── solobuddy.json
├── personal-site.json
├── optimi-mac.json
├── reelstudio.json
├── parsertang.json
└── pointg.json
```

## Appendix B: Voice Reference

**Jester-Sage (из PROFILE.md):**

| Aspect | Description |
|--------|-------------|
| Tone | Ironic, Raw, Philosophical |
| Style | Honest process sharing |
| Humor | Self-deprecating, observational |

**Avoid:** "Excited to announce! 🚀🎉"
**Use:** "Spent 3 hours debugging. The bug was a typo. The typo was mine."
