# 🎯 Proactive Buddy Implementation

## Задача

Реализовать **Git Watcher** — первую "живую" функцию SoloBuddy.  
Buddy должен анализировать Git-репозитории и генерировать наблюдения.

---

## Конечный результат

При открытии Hub (`http://localhost:3000`) пользователь видит блок с сообщением:

> 💭 "Заметил активность в personal-site — 8 коммитов вчера. VOP тихо уже 3 дня."

---

## Что нужно сделать

### 1. Создать структуру данных

```
bip-buddy/data/
├── projects.json   # конфиг проектов для отслеживания
├── activity.json   # кэш последней активности (генерируется)
└── messages.json   # очередь сообщений Buddy (генерируется)
```

**projects.json** — список проектов с путями к Git-репозиториям:
```json
{
  "projects": [
    { "name": "SPHERE", "path": "/Users/admin/projects/personal-site" },
    { "name": "VOP", "path": "/Users/admin/projects/vob" },
    { "name": "ReelStudio", "path": "/Users/admin/projects/reelstudio" }
  ]
}
```

### 2. Git Watcher модуль (`hub/watcher.js`)

| Функция | Что делает |
|---------|------------|
| `scanProject(projectPath)` | Выполняет `git log --since="7 days ago"`, парсит JSON |
| `getActivityStats(project)` | Возвращает: commits за день, последний коммит, дней тишины |
| `generateInsight(stats)` | По шаблону генерирует текст наблюдения |

**Паттерны для insights:**
- Нет коммитов 3+ дня → "X притих уже N дней"
- 5+ коммитов за день → "Активный день в X — N коммитов!"
- Первый коммит после паузы → "О, ты вернулся к X!"

### 3. API endpoint (`hub/server.js`)

```javascript
// GET /api/buddy-message
// Возвращает последнее сообщение от Buddy
{
  "message": "Заметил активность в personal-site — 8 коммитов вчера.",
  "timestamp": "2026-01-09T16:00:00Z",
  "type": "observation"
}
```

### 4. UI блок (`hub/index.html` + `styles.css`)

Добавить блок перед основным контентом:

```html
<div class="buddy-message" id="buddy-message">
  <div class="buddy-avatar">🧬</div>
  <div class="buddy-content">
    <p class="message-text"></p>
    <span class="message-time"></span>
  </div>
</div>
```

Стили: мягкий, тёплый блок в духе "Warm Hearth" (см. существующий `styles.css`).

### 5. Frontend (`hub/app.js`)

Добавить `fetchBuddyMessage()` и рендер в `loadAllData()`.

---

## Контекст проекта

| Файл | Содержание |
|------|------------|
| [living-buddy-concept.md](file:///Users/admin/projects/bip-buddy/ideas/living-buddy-concept.md) | Философия "живого компаньона" |
| [hub/server.js](file:///Users/admin/projects/bip-buddy/hub/server.js) | Существующий Express сервер |
| [hub/styles.css](file:///Users/admin/projects/bip-buddy/hub/styles.css) | "Warm Hearth" тема |
| [hub/app.js](file:///Users/admin/projects/bip-buddy/hub/app.js) | Frontend логика |

---

## Ограничения

- **Не использовать LLM** для генерации текста — только шаблоны
- **JSON файлы** для хранения (не SQLite)
- **Запуск при старте сервера** (не cron)
- Проверить, что путь к репо существует, перед вызовом git

---

## Порядок выполнения

1. [ ] Создать `data/projects.json` с конфигом
2. [ ] Написать `hub/watcher.js` (scanProject, getActivityStats)
3. [ ] Добавить `generateInsight()` с шаблонами
4. [ ] Добавить `GET /api/buddy-message` в server.js
5. [ ] Добавить UI блок в index.html
6. [ ] Добавить стили для `.buddy-message`
7. [ ] Подключить fetch в app.js
8. [ ] Протестировать в браузере

---

## Запуск

```bash
cd /Users/admin/projects/bip-buddy/hub
npm start
# Открыть http://localhost:3000
```

---

## Критерий успеха

При загрузке Hub отображается сообщение Buddy, основанное на реальных Git-данных из подключённых проектов.
